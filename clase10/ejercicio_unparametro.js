const multiply = (a,b) => a * b;

const duplicate = a => multiply(2 , a);
const triplicate = a =>multiply(3 , a)

console.log(multiply(2,3))
console.log(duplicate(2))
console.log(triplicate(3))

// currying buscar info 